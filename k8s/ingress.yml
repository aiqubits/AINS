apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ains-ingress
  namespace: ains
  annotations:
    # Set to "true" after deploying TLS certificates (see tls section below)
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    # Preserve long-lived `/api/ai/response` SSE streams through the outer
    # ingress hop. These settings are safe for the regular JSON routes too.
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  # ==========================================
  # TLS configuration (uncomment to enable HTTPS)
  #
  # Prerequisites:
  # 1. Create a TLS secret:
  #      kubectl create secret tls ains-tls \
  #        --cert=/path/to/cert.pem \
  #        --key=/path/to/key.pem \
  #        -n ains
  # 2. Or use cert-manager to auto-provision Let's Encrypt certificates.
  # ==========================================
  # tls:
  # - hosts:
  #   - ains.local
  #   secretName: ains-tls
  rules:
  - host: ains.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ains-web-service
            port:
              number: 80
